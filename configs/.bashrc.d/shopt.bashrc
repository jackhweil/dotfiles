# shopt.bashrc
#
# Set general shell options for bash

shopt -s cdable_vars    # allow us to define some shortcut variables for common paths
shopt -s cdspell        # try to fix minor spelling mistakes when cd'ing to paths that don't exist
shopt -s dirspell       # try to fix minor spelling mistakes when autocomplete'ing to paths that don't exist
